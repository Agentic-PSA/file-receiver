# API key for authentication (shared with Lovable Cloud edge function)
FILE_RECEIVER_API_KEY=your-secure-api-key-here

# Path on host where files will be stored (bind mount)
HOST_STORAGE_PATH=/srv/bluebox/files

# Max upload size in MB
MAX_FILE_SIZE_MB=500
